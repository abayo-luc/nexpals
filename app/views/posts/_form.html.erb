<!DOCTYPE html>
<html>
<head>
  <title>NextPals Posts</title>
  <meta charset="utf-8">
  <script src="https://cdn.ckeditor.com/4.6.2/standard/ckeditor.js"></script>
</head>
<body>
<%= javascript_include_tag Ckeditor.cdn_url %>
  <%= form_for @post do |f| %>
  <% if @post.errors.any? %>
  <div class="errors">
   <h2><%= pluralize(@post.errors.count, "error") %>Prevented this post from saving</h2>
   <ul><%= @post.errors.full_messages do |message| %>
     <li><%= message %></li>
     <% end %>
   </ul>
  </div>
  <% end  %>

    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title, style:"width:100%; font-family:cursive;" %>
    </p>
    <p>
      <%= f.label :all_tags, "Tags (separated by commas)" %><br>
      <%= f.text_field :all_tags%>
    </p>
    <p>
      <%= f.label :intro %><br>
      <%= f.text_area :intro, style: "width:100%; font-style:italic;" %>
    </p>
    <p>
      <%= f.label :body %>
      <%= f.cktext_area :body, :class => 'ckeditor' %>
    </p>
      <p>
        <%= f.submit %>
      </p>
      <% end %>
</body>
</html>